$newline always
<div .medium>
  <form method="post" enctype=#{enctype}>
    <h3>Album settings
    ^{albumSettingsWidget}
  $if ownerPresence == True
    <a href=@{AlbumDeleteR albumId}>Delete this album
